import { getMovie } from './yts-api';
import $ from 'jquery';

// const movie = await getMovie('tt5013056');
// console.log(movie);

export default () => {
  $(document).ready(() => {
    console.log('test');
  })
};
